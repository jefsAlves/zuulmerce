spring.application.name=ms-computing
server.port=${PORT:0}

#Configuration name and door random Eureka
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance.intance_id:${random.value}}

#Configuration Eureka Client
eureka.client.service-url.defaultZone=http://localhost:8761/eureka